langauge: node_js
node_js:
  - 14.4.0
cache: npm

install:
  - npm install

# script:
#   - npm test
#   - npm run build

deploy:
  provider: heroku
  skip-clearnup: true
  github-token: $GITHUB_TOKEN